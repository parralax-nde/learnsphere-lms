# JWT secret for signing auth tokens – use a strong random value in production
JWT_SECRET=change-me-in-production

# Google OAuth credentials (required for social account linking in production)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Application URL
# NEXT_PUBLIC_APP_URL=http://localhost:3000
